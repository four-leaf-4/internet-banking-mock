import React from "react";
import { Box, Typography } from "@material-ui/core";

export const CompleteText = () => {
  return (
    <Box my={3}>
      <Typography align="center">申請完了しました。</Typography>
      <Typography align="center">ご利用ありがとうございます。</Typography>
    </Box>
  );
};
